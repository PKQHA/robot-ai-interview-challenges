# AI 使用说明

## 使用的工具

在需求分析、实现、测试和文档编写过程中使用了 OpenAI Codex。已安装的 `testing-best-practices` skill 用于指导公共接口行为测试和确定性时间测试；`ros2-engineering-skills` 参考资料仅用于组织 ROS 2 证据分析。

## AI 提供的帮助

- 将题目整理为范围明确的交付和测试检查表。
- 提议应用只维护四项必要的可变状态。
- 实现公共数据模型和事件处理逻辑。
- 根据业务规则和题目提供的时间线生成 pytest 测试场景。
- 起草模块边界说明，以及 ROS 2 的事实、推断和待验证项分析。

没有使用 AI 查看或复制其他候选人的提交。

## 可由人工验证的设计决策

- 忙碌期间离场计时到期时静默确认离场，之后不补发效果。
- 对话与会议使用独立状态，因为两者可能重叠。
- `person_id` 保留在公共模型中，但不增加题目未定义的多人策略。
- ROS 2 位于业务模块之外，只在书面诊断中分析。

## 验证方式

- 将实现和测试逐条对照题目要求重新检查。
- 在本地运行完整 pytest 测试套件，最终结果为 `10 passed`。
- 测试使用明确的固定事件时间戳，不等待真实时间，也不调用网络。
- 检查公共导入路径和方法签名均未改变。
- 检查最终文件列表，确认没有引入题目禁止的集成或不必要抽象。
