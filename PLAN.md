# 实施计划

## 范围

按照题目要求，实现一个纯内存的机器人迎宾应用，并保留指定的公共导入路径。本项目不包含 ROS 2、硬件、网络、数据库或 Web 集成。

## 实施步骤

1. 定义不可变的 `Event` 和 `Effect` 数据模型。
2. 在 `RobotApplication` 中实现最小的接待、离场计时、对话和会议状态。
3. 使用 pytest 覆盖每条必需业务规则和题目给出的完整事件时间线。
4. 运行完整测试套件并记录准确结果。
5. 说明模块边界、ROS 2 证据分析、AI 使用情况以及尚未完成的提交步骤。

## 关键决策

- 将输入事件解释为一个迎宾区域内连续的一次在场接待过程；题目提供的 `person_id` 保留在公共接口中，供未来明确提出多人需求时使用。
- 如果离场计时在对话或会议抑制期间到期，则静默完成离场确认，之后不补发效果。
- 语音使用 `effect_type="SPEECH"`。题目要求送客但没有规定具体文案，因此送客语使用 `"感谢光临，欢迎下次再来"`。

## 验证方式

运行：

```text
python -m pytest -q
```

成功标准：所有必需行为测试均通过，且没有跳过的测试。
